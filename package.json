{
  "name": "tts",
  "version": "1.0.0",
  "description": "server super tts",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --trace-warnings",
    "build": "electron-builder -c project/install/install.yml"
  },
  "author": "taosifan",
  "license": "ISC",
  "devDependencies": {
    "electron": "^19.0.0",
    "electron-builder": "^23.0.3"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
